import { Counter } from '@/components/Counter'

export default function App() {
  return (
    <main style={{ padding: 16, fontFamily: 'system-ui, sans-serif' }}>
      <h1>Demo Site</h1>
      <p>Production-ready Vite + React + TypeScript scaffold.</p>
      <Counter />
    </main>
  )
}
